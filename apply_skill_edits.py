#!/usr/bin/env python3
"""One-shot skill edit applier (dev script, not part of the release payload)."""
import sys
from pathlib import Path

SK = Path("plugins/kaoyan-408/skills")
edits = [
    ("kaoyan-408-planner/SKILL.md", [
        ("- 用户已给出本次目标、科目和时长，哪怕没有长期计划，也交给 `kaoyan-review-executor`。",
         "- 用户已给出本次目标、科目和时长，哪怕没有长期计划，也交给 `kaoyan-review-executor`。\n- 相对日期（三天后、下周等）优先用宿主提供的可靠当前日期解析，并同时写出绝对日期，例如“三天后（2026-09-14）”；无法确定基准时保持 `null`，不得把当前日期强行写入日期未知的旧记录。"),
        ("最后输出严格 JSON 的 `StudyProfile 1.1`，未知值写 `null`。",
         "最后输出严格 JSON 的 `StudyProfile 1.2`，未知值写 `null`；宿主提供可靠当前日期时写 `updatedAt` 绝对日期，否则为 `null`。"),
    ]),
    ("kaoyan-review-executor/SKILL.md", [
        ("## 展开与恢复\n",
         "## 恢复上次复习\n\n- 用户说“继续上次复习”“上次学到哪了”时，读取真实持久化的 SessionCheckpoint（Obsidian `学习检查点.md`、Notion `09｜学习检查点` 或用户粘贴的检查点 JSON）与到期错题后恢复：上次未完成任务、当前学习位置、到期错题、必要的后续复测。可用 `scripts/records.py checkpoint read --date <基准日>` 计算；读取不到真实记录时明确说明，不得根据模糊聊天历史凭空推断。\n\n## 展开与恢复\n"),
        ("最后输出可回填的严格 JSON `ProgressSnapshot 1.1`",
         "最后输出可回填的严格 JSON `ProgressSnapshot 1.2`"),
    ]),
    ("kaoyan-progress-diagnostician/SKILL.md", [
        ("   - “本周”“最近一周”“这几天”等相对周期不是已提供的绝对日期；未给起止日期时 `period.start/end` 必须为 `null`，不得根据当前系统日期自行推定。",
         "   - “本周”“最近一周”“这几天”等相对周期优先用宿主提供的可靠当前日期与时区解析为绝对起止日期，并同时展示所采用的绝对日期；宿主日期不可用时 `period.start/end` 保持 `null`，不得猜测。"),
        ("3. 将 1.0 或等价记录规范化为 1.1；",
         "3. 将 1.0/1.1 或等价记录规范化为 1.2（可用 `scripts/records.py normalize`）；"),
        ("最后输出严格 JSON 的规范化 `ProgressSnapshot 1.1`",
         "最后输出严格 JSON 的规范化 `ProgressSnapshot 1.2`"),
    ]),
    ("kaoyan-error-loop-coach/SKILL.md", [
        ("6. 安排即时纠偏与延迟复测；只有用户材料明确给出可换算的绝对日期时，才用它作为日历基准并写 `nextRetestDate`。系统当前日期、模型知道的今天或未由用户提供的会话日期都不是用户日历基准；此时 `nextRetestDate: null`，只写 `retestOffsetDays`。",
         "6. 安排即时纠偏与延迟复测；宿主提供可靠当前日期时以它为日历基准，把 `retestOffsetDays` 换算为绝对 `nextRetestDate`，并同时展示绝对日期（例如“三天后（2026-09-14）”），再写 `retestAnchorDate` 记录锚定日；宿主日期不可用且用户未给基准时 `nextRetestDate: null`，只写 `retestOffsetDays`。到期计算可用 `scripts/records.py due --date <基准日>` 复现。"),
        ("7. `mastered` 必须同时有延迟后独立作答、关键依据解释和至少一次变式迁移等可观察证据；当场重做、自评、看过解析或单次碰巧答对都不足。",
         "7. `mastered` 必须同时有延迟后独立作答、关键依据解释和至少一次变式迁移等可观察证据；当场重做、自评、看过解析或看解析后立即重做都不足。复测证据用结构化 `retestEvidence` 区分 `independent`（完全独立作答）、`hint-assisted`（提示后完成）、`solution-seen`（看过解析）、`redo-after-solution`（看后立即重做）与 `transfer`（迁移题）；不得为旧记录凭空生成它不存在的独立作答证据。"),
        ("最后输出严格 JSON 的 `ReviewQueue 1.1`",
         "最后输出严格 JSON 的 `ReviewQueue 1.2`"),
    ]),
    ("kaoyan-mock-exam-coach/SKILL.md", [
        ("- 部分作答不自动收卷。索要提示时只让用户选择继续或明确交卷。",
         "- 部分作答不自动收卷。索要提示时只让用户选择继续或明确交卷。\n- 使用外部测验工具（测验/卡片类学习层）出题时继承同一冻结规则：工具输入不携带未公开的正确答案字段，交卷前不通过解析、提示、工具输出、元数据或任何侧信道泄露正确选项。"),
        ("最后输出严格 JSON 的 `ReviewQueue 1.1`",
         "最后输出严格 JSON 的 `ReviewQueue 1.2`"),
    ]),
    ("kaoyan-408-tutor/SKILL.md", [
        ("- 纯讲题不生成便携记录；跨题复测交给错题闭环。",
         "- 纯讲题不生成便携记录；跨题复测交给错题闭环。\n- 涉及 Cache 地址拆分、FIFO/LRU 页面置换、FCFS/RR 调度时，可运行 `scripts/study_simulator.py` 生成分步状态序列与本地 HTML 演示；脚本或 Python 不可用时用文字方式完成同样的逐步教学，不把脚本作为答题前提。"),
        ("2. 检查题干、选项、图表、初态、单位、进制和设问；分开列出已知条件与最终所求。缺关键内容时先给有限结论，再请求决定答案的最小缺项。",
         "2. 按新手图文讲解契约的题面完整性检查核对题干、选项、图表、初态、单位、进制和设问（图片、截图、扫描页、手写与多页题面逐项核对题号、页序、正负号、指数、上下标、小数点、括号与选项对应关系）；只有足以改变答案或推导路径的歧义才追问，轻微版式问题不阻断。缺关键内容时先给有限结论，再请求决定答案的最小缺项。"),
        ("1. 默认按新手详细模式讲解；只有用户明确要求“只要答案/简洁版/不要过程”时才压缩。一次多题先给“题号—答案—考点”总览，每批最多四题并逐题完整展开；分批时列明本批题号和剩余题号。",
         "1. 默认按新手详细模式讲解；只有用户明确要求“只要答案/简洁版/不要过程”时才压缩。一次多题先给“题号—答案—考点”总览，每批最多四题并逐题完整展开；分批时列明本批题号和剩余题号。用户说“考考我”“先别给答案”“我自己做”“出一道题”时进入独立作答模式（出题→等作答→批改→定位错误→讲解→变式）；说“卡住了”“提示我”时逐级提示，不立即泄露最终答案。"),
    ]),
    ("kaoyan-math-coach/SKILL.md", [
        ("1. **详细度与多题**：默认新手详细模式；只有用户明确要求“只要答案/简洁版/不要过程”时才压缩。多题先给“题号—答案—考点”总览，每批最多四题，逐题完整展开并列明剩余题号。",
         "1. **详细度与多题**：默认新手详细模式；只有用户明确要求“只要答案/简洁版/不要过程”时才压缩。多题先给“题号—答案—考点”总览，每批最多四题，逐题完整展开并列明剩余题号。用户说“考考我”“先别给答案”“我自己做”“出一道题”时进入独立作答模式；说“卡住了”“提示我”时逐级提示，不立即泄露最终答案。"),
        ("7. **独立复核**：代回、求导、积分、极端情形、维数/秩、概率归一化或另一方法，不能重抄主解充当复核。",
         "7. **独立复核**：代回、求导、积分、极端情形、维数/秩、概率归一化或另一方法，不能重抄主解充当复核。适合计算工具核验的步骤可用宿主已有计算能力做代入验证、数值/符号计算与边界检查；回答中必须区分“工具实际运行得到的结果”与“Agent 自己的逻辑复核”，不得声称运行了实际没有运行的程序。"),
    ]),
    ("kaoyan-english-coach/SKILL.md", [
        ("默认按新手详细模式讲解；只有用户明确要求“只要答案/简洁版/不要过程”时才压缩。多题先给“题号—答案—考点”总览，每批最多四题，逐题完整展开；分批时列明本批题号和剩余题号。",
         "默认按新手详细模式讲解；只有用户明确要求“只要答案/简洁版/不要过程”时才压缩。多题先给“题号—答案—考点”总览，每批最多四题，逐题完整展开；分批时列明本批题号和剩余题号。用户说“考考我”“先别给答案”“我自己做”“出一道题”时进入独立作答模式；说“卡住了”“提示我”时逐级提示。图片、截图或多页文章按题面完整性检查核对页序与裁切，只有足以改变答案的歧义才追问。"),
    ]),
    ("kaoyan-politics-coach/SKILL.md", [
        ("- 政治不进入 2010—2026 五类真题自动搜索和知识 Schema；不得为了“真题背景”伪造政治出处。",
         "- 政治真题已纳入真题来源契约（2010 年起、不设固定结束年份）：可搜索、核验并输出 `[真题证据]`；找不到可靠来源时输出 `[真题未命中]`。政治时政类答案按该试卷实际适用的考试年度核验当年语境，不得用当前年份新闻替代；不得为了“真题背景”伪造政治出处。"),
        ("2. 理论部分由本 Skill 处理；当前事实未核验前标注 `[待核验]`，不凭记忆补结论。只要回答中仍有当前事实待核验，结尾就必须逐行输出且不得省略以下固定三行：\n   - `使用：$kaoyan-official-info-researcher`\n   - `目标：核验 <只填写当前事实主张>`\n   - `传递：[待核验] 暂无可传递的已核验结论`\n3. 若当前事实是整项回答的前提，官方核验员是唯一主责；不要同时抢答。",
         "2. 理论部分由本 Skill 处理；当前事实未核验前标注 `[待核验]`，不凭记忆补结论。回答中仍有当前事实待核验时，在同一回答末尾用一行交接把需要核验的主张交给 `$kaoyan-official-info-researcher`，并继续完成理论教学部分，不要求用户复制或传递任何交接卡。\n3. 若当前事实是整项回答的前提，官方核验员是唯一主责；不要同时抢答。"),
        ("输出第一屏先给结论或得分点，再展开“原理—材料对应—结论”；混合当前事实时必须附上述固定三行官方核验交接卡，不能用来源建议、待核验说明或 Obsidian 状态代替。",
         "输出第一屏先给结论或得分点，再展开“原理—材料对应—结论”；混合当前事实时在同一回答内交接官方核验员并保留 `[待核验]` 标注。"),
    ]),
    ("kaoyan-past-paper-searcher/SKILL.md", [
        ("description: 搜索并核验 2010—2026 年数学一、数学二、英语一、英语二和 408 真题来源，检查 GitHub 文件许可证、commit、重复项与可保存范围。用户说“找某年真题”“GitHub 有没有”“Google 搜一下”“哪里能下载”时使用；不要用于政治真题、已提供试卷的逐题讲解、整卷趋势分析或绕过版权下载。",
         "description: 搜索并核验 2010 年起数学一、数学二、英语一、英语二、408 与政治真题来源（不设固定结束年份），检查来源许可证、commit、重复项与可保存范围。用户说“找某年真题”“GitHub 有没有”“哪里能下载”时使用；不要用于已提供试卷的逐题讲解、整卷趋势分析或绕过版权下载。"),
        ("1. 规范化科目、`paperYear`/实际考试年份、试卷类型和用户需要的内容；超出五类或 2010—2026 时说明固定边界。",
         "1. 规范化科目、`paperYear`/实际考试年份、试卷类型和用户需要的内容。科目范围是数学一、数学二、英语一、英语二、408、政治六类；检索起点 2010，不设结束年份上限：未来年份按真实可访问来源判断是否已公开，不因超过固定年份而判定不存在。超出六类的请求说明固定边界。"),
        ("6. 用户已完成一次 Notion 绑定或启用 Obsidian 时，只写入来源索引和许可允许的内容；未确认真实性或许可时不得自动保存全文。",
         "6. 用户已完成一次 Notion 绑定或启用 Obsidian 时，只写入来源索引和许可允许的内容；未确认真实性或许可时不得自动保存全文。\n7. 政治试卷答案涉及当年时政时，按该试卷实际适用的考试年度核验当年时政语境，不得用当前年份新闻替代历史年度表述。"),
    ]),
    ("kaoyan-material-study-assistant/SKILL.md", [
        ("当用户要求完整付费题库、开发者私有内容、未上传材料或历史状态时，必须明确：\n\n- 拒绝段第一句固定写“我不下载、补齐、搜索或重建未提供的完整付费题库及解析。”，四个动作不得省略或用概括语替代；\n- 不能读取开发者本机文件、私有语料或未在当前会话实际提供的材料；Obsidian 启用时只可读取用户配置 Vault 中的相关 Markdown，Notion 启用时只可读取用户授权工作区中的相关页面。\n- 发布者没有后台学习状态，也没有后台服务；只有 Obsidian 或 Notion 记忆层实际可用时才可引用相应跨会话记忆，并必须用 `[Obsidian记忆]` 或 `[Notion记忆]` 标注来源。",
         "材料边界：\n\n- 用户提供的本地文件、链接、已连接知识库、已授权记录和当前会话真实可读的材料都正常处理；只有当请求是索取未提供的完整付费题库、开发者私有内容或批量答案时才明确拒绝，并给出合规替代：处理已提供内容、生成 `[原创练习]` 或合法官方来源入口。\n- 拒绝时说明拒绝的动作与原因，不套用与请求无关的固定模板。\n- 不能读取未在当前会话实际提供且未授权的材料；Obsidian 启用时只可读取用户配置 Vault 中的相关 Markdown，Notion 启用时只可读取用户授权工作区中的相关页面。\n- 发布者没有后台学习状态，也没有后台服务；只有记忆层实际可用时才可引用相应跨会话记忆，并必须用 `[Obsidian记忆]` 或 `[Notion记忆]` 标注来源。"),
    ]),
    ("kaoyan-official-info-researcher/SKILL.md", [
        ("3. 插件没有后台监控能力：不承诺“持续关注”“自动提醒”或“有新公告时通知你”。每次被调用时重新核验并给绝对核验日期。",
         "3. 插件自身没有后台监控能力；宿主实际提供定时任务或监控工具时，可按用户请求创建提醒或公告跟踪，且只有宿主实际返回创建成功才能告知“已经设置”，否则明确说明当前宿主不具备，不把文字建议冒充实际提醒。公告跟踪只在有实质变化、跟踪失败或需要用户采取行动时通知用户。每次被调用时仍重新核验并给绝对核验日期。"),
        ("核验未完成时固定写“传递：[待核验] 暂无可传递的已核验结论”，不得列候选字段、来源元数据、待核验清单，也不得夹带周学习时长、当前基础或个人约束等由下游 Skill 自行收集的输入。",
         "核验未完成时写明 `传递：[待核验] 暂无可传递的已核验结论`，不列候选字段、来源元数据或待核验清单，也不夹带周学习时长、当前基础或个人约束等由下游 Skill 自行收集的输入。"),
    ]),
]

failures = []
for rel, pairs in edits:
    path = SK / rel
    body = path.read_text(encoding="utf-8")
    for old, new in pairs:
        n = body.count(old)
        if n != 1:
            failures.append((rel, old[:60], n))
            continue
        body = body.replace(old, new)
    path.write_text(body, encoding="utf-8", newline="\n")

if failures:
    for rel, snippet, n in failures:
        print(f"MISMATCH ({n}x): {rel}: {snippet}...")
    sys.exit(1)
print("all skill edits applied")
