# 版本记录

本项目遵循 [Semantic Versioning](https://semver.org/)。

## [2.5.0] - 2026-09-11

### 新增

- 便携学习记录 **Schema 1.2**：稳定记录 `recordId`（一次分配持续复用）、`updatedAt`、结构化 `retestEvidence`（区分完全独立作答 / 提示后完成 / 看过解析 / 看后立即重做 / 迁移题）与轻量 `SessionCheckpoint`；读取端继续兼容 1.0/1.1/1.2，**不需要提前迁移整个旧库**。
- 统一记录 CLI `scripts/records.py`（纯标准库）：`validate` / `normalize` / `merge` / `due` / `checkpoint`；`merge` 冲突确定性合并不丢数据，`due` 必须显式传基准日期，`normalize` 不伪造历史证据。
- 408 状态模拟器 `scripts/study_simulator.py`（纯标准库）：Cache 地址拆分、FIFO/LRU 页面置换、FCFS/RR 调度的分步状态模拟与本地 HTML 演示；已知正确答案均有自动化测试，脚本不可用时 Skill 仍用文字方式教学。
- 三种教学模式写入公共讲解契约：详细讲解（默认）、逐级提示、独立作答（“考考我”“先别给答案”等触发）；统一答案泄露规则，冻结模考交卷前零泄露且所有外部测验工具继承同一规则。
- 题面完整性检查：图片、截图、扫描页、手写与多页题面统一核对题号、页序、正负号、指数、单位、进制、选项对应等；只有足以改变答案的歧义才追问。
- 真题检索扩展为**六科**（数学一/二、英语一/二、408、政治），检索起点 2010 并**取消固定结束年份上限**；未来年份按真实可访问来源判断，政治时政答案按试卷实际适用年度核验。
- “继续上次复习”：读取真实持久化的 `SessionCheckpoint` 与到期错题恢复，读取不到时明确说明；Obsidian `学习检查点.md` 与 Notion `09｜学习检查点` 落点就绪。
- 消费者固定版本安装验证 `install_local.py verify-release` / `verify-tree`：验证来源、结构、版本、文件哈希，**与发布时长无关**（31 天、一年后哈希正确仍可安装）；安装器改用官方 CLI 结构化 JSON 输出识别 Git/本地 marketplace，缓存目录不再被误认为仓库来源。
- GPT-6 实际回答验收 harness 与 17 条测试材料（`eval/gpt6-acceptance/`），截至发布**尚未实际运行**。

### 调整

- 12 个品牌专属学习契约合并为一个 [通用学习层契约](plugins/kaoyan-408/references/learning-layer-contract.md)（搜索/计算/词典/卡片/测验/图谱/媒体/笔记/课程/文件/定时任务 11 个能力族）；用户点名品牌且工具可用时仍按需调用，真实工具能力不变。
- 权限语义严格区分：`只读`/`本次不记忆` 禁止所有学习记录写入；`不要同步 Notion` 仅限制 Notion，不影响已授权的 Obsidian 等本地持久化。
- 相对日期优先用宿主可靠当前日期解析并同时展示绝对日期；到期计算支持显式基准日期；不再把当前日期强行写入日期未知的旧记录。
- 提醒与公告跟踪：宿主实际提供定时任务工具时按需创建，只有宿主实际返回创建成功才告知“已经设置”；公告跟踪只在有实质变化、失败或需要用户行动时通知。
- 院校比较先统一口径（招生年度、院系、专业代码、学硕/专硕、全日制/非全、考生类别），九类招生指标不得混用，未公开数据留空或标未知；独立检索可交子 Agent 并行，最终汇总与写入由主 Agent 完成。
- 同一会话内连续任务（检索→核验→讲解→笔记）自动串联，不再要求用户复制粘贴“交接卡”；政治教练的固定三行交接、安装文档 `--check` 等形式性负担移除，统一为 `--validate-only`。
- 脆弱测试清理：不再固定路由/行为场景总数，改为结构校验 + 全 Skill 覆盖 + 唯一 ID；保留并强化密钥泄漏、路径保护、Schema 校验、发布完整性与真实行为测试，新增 6 条 v2.5.0 行为用例。

### 测试

- 新增 records（规范化/ID 稳定性/合并/到期/检查点/权限）、模拟器（Belady 异常、LRU 经典 12 缺页、FCFS 平均等待、RR 时间片边界到达、预装页面、Cache 边界）、安装器（JSON 来源识别、缓存目录拒绝、31 天/一年验证、损坏与异源拒绝）与 GPT-6 harness 校验的自动化测试。
- 离线门禁继续保留：官方插件/Skill 校验、Windows/Ubuntu 双平台、可重复构建、ZIP 字节一致与 SHA-256。

### 移除

- 移除 12 个重复品牌契约文件（内容并入通用学习层契约）；移除“每题强制输出真题搜索失败标签”“与任务无关的连接状态说明”“固定拒绝模板逐字要求”等形式性负担。

## [2.4.0] - 2026-08-24

### 调整

- 数学一/二、英语一/二、408 与政治的单题和概念讲解默认使用新手详细模式；用户明确要求“只要答案”“简洁版”或“不要过程”时可覆盖为简洁回答。
- 多题回答先给答案与考点总览，每批最多四题并逐题完整展开；分批只发生在题目边界，不通过省略推导压缩篇幅。
- 公共讲解契约新增已知条件与目标、解题路线、关键中间结果、选择题逐项核对，以及“第一处错误/最早易错点”的区分。
- 四科学科 Skill 分别强化计算与编码过程、数学独立验算、英语句法证据链和政治原理—材料—得分点映射。

### 测试

- 保持 13 个 Skills、65 个路由用例和 52 个行为用例不变；重写详细讲解回归用例，覆盖 408 四题同批完整展开、数学显式简洁覆盖、英语逐项证据链和政治规范作答。
- 仓库校验器新增详细讲解契约与四科学科 Skill 的必需标记检查，防止后续退化为只报答案或省略关键步骤。

## [2.3.0] - 2026-08-08

### 新增

- 新增 Exa 资料检索契约：宿主提供工具时检索网页、论文与备考资料，结果仅作为线索，不替代一手官网。
- 新增 Wolfram 计算核验契约：对数学公式、数值与符号计算执行独立核验，结果与模型推导分离。
- 新增 A-Z Dictionary 词典契约：查询术语与考研常见表达，释义与例句只做必要引用。
- 新增 Quizlet 闪卡记忆契约：按知识点生成或检索闪卡组，学习结果可沉淀进便携记录。
- 新增 Ace Quiz Maker 章节测契约：按科目、章节与难度生成原创自测，与模考 Skill 互补。
- 新增 Ace Knowledge Graph 知识图谱契约：生成知识点关系图，配合新手图文讲解。
- 新增 AhaMotion 视频讲解契约：对概念与算法生成讲解视频，失败时保留图文版。
- 新增 Vocabulary Trainer 词汇训练契约：按英语一/二核心词整理词表与训练。
- 新增 Kahoot 互动复习契约：生成互动选择题复习回合，题目标为 [原创练习]。

### 调整

- 能力路由契约条件式加载扩展为 14 个学习层，统一“学习层状态”输出。
- 证据与版权契约补充 9 个新增学习层的授权与版权边界。
- README、PRIVACY、TERMS 更新：各应用均为用户授权的可选学习层，发布者不接收其登录态、会话或导出数据。

## [2.2.0] - 2026-08-07

### 新增

- 新增 Udemy 课程资源契约：宿主提供已授权 Udemy 工具时检索课程、章节与讲义，支持“课程章节对应考试范围”“按课程排计划”等场景。
- 新增 Sider Scholar 资料检索契约：宿主提供工具时检索学术论文与扩展资料，结果仅作为线索，不替代一手官网。
- 新增 GoodNotes 笔记记忆契约：宿主提供工具时直连读写手写笔记；否则支持用户导出 Markdown/PDF 兜底，不解析私有格式。

### 调整

- 能力路由契约改为条件式加载五层学习层（Obsidian / Notion / Udemy / Sider Scholar / GoodNotes），并统一“学习层状态”输出。
- 证据与版权契约补充 Udemy 课程、Sider Scholar 论文与 GoodNotes 笔记的授权与版权边界。
- README、PRIVACY、TERMS 更新：三应用均为用户授权的可选学习层，发布者不接收其登录态、会话或导出数据。


## [2.1.0] - 2026-08-07

### 新增

- `kaoyan-official-info-researcher` 支持“用户指定 + 研招网硕士专业目录发现”定位考 408 的院校，并逐校官网核验招生目录、复试内容与最近两个招生年度录取情况。
- 新增事项与一手来源映射（统考大纲、专业目录、报名、初试安排、复试内容、录取情况、时政政策）和院校核验结果表。
- 涉及报名、大纲、调剂、复试安排时同步检查研招网最新公告栏。

### 调整

- 录取情况只提取复试线、拟录取人数、分数区间等统计口径并给直接 URL；不复制含考生个人信息的完整拟录取名单，无官方报录比时标 `[待核验]`。
- 明确插件无后台监控能力，不承诺持续关注或自动提醒；已核验快照可按大脑契约写回 Obsidian/Notion。
- manifest 中的仓库 URL 迁移到 `yq6666-66/408-codex-plugin`，不再依赖旧仓库名重定向。
- 修正 `QUALITY_GATES.md` 中残留的旧用例数量描述。

### 测试

- 路由用例保持 `65`：`official-P2` 改为拒绝用真题替代当前官方范围的反捷径用例，`official-L1` 覆盖院校目录发现、复试与两年录取的口语化请求。
- 行为用例保持 `52`：`behavior-48` 重写为院校目录发现、复试/近两年录取核验与无后台监控声明的多轮场景；`behavior-11`、`behavior-36` 扩展院校信息口径。

## [2.0.0] - 2026-08-04

### 变更

- 插件 ID、目录和 Marketplace 名称改为 `kaoyan-408`，显示名改为“408考研插件”。
- 数学与英语 Skills 扩展为数学一/二、英语一/二；规划 Skill 改名为 `kaoyan-408-planner`。
- 默认学科讲解改为真题可追溯的新手图文结构；无可靠来源时明确使用原创示例。
- Obsidian 配置升级至 Schema 1.1，并提供保留旧私人目录的 1.0 迁移；Notion 改为一次绑定后在确认范围内自动增量更新。

### 新增

- 新增 `kaoyan-past-paper-searcher`，固定发现 2010—2026 年数学一、数学二、英语一、英语二和 408 公开来源。
- 新增真题来源与版权契约、新手图文讲解契约及可机器验证的真题知识 Schema。
- 路由和行为离线场景扩展为 `65/65` 与 `52/52`。

### 安全

- 网页、搜索结果和 GitHub 仓库一律按不可信输入处理；许可证不明时禁止保存全文。
- 发布包继续排除真题、搜索缓存、Notion 页面 ID、Vault 路径和私人学习记录。

## [1.4.0] - 2026-08-03

### 新增

- 增加可选 Notion 学习大脑契约：在用户授权的工作区中检索相关页面，并按对话内容沉淀最小、可复用的学习增量。
- 增加 Notion 与 Obsidian 的来源分离、只读覆盖、目标页面确认、并发合并和写入失败降级规则。

### 调整

- 12 个 Skills 通过公共路由契约条件式接入 Notion，不新增总控 Skill、MCP、后台服务或发布者数据库。
- 发布门禁改为完全离线、可重复的静态验证、单元测试、安全扫描、官方 validator 与跨平台一致构建；不再要求登录 Codex CLI。
- 增加 GitHub Actions 手动发布后备入口；无需本地 GitHub/Codex CLI 登录，且仍强制最新 `main`、manifest 标签匹配和完整跨平台门禁。
- 保留 60 个路由和 36 个行为场景作为规则覆盖资产，验证其结构、路由、rubric 与 Schema 完整性，不再声称模型运行通过率。

### 移除

- 移除 Codex CLI 模型评测器、响应缓存、前向证据、离线签名器、公钥变量和 `pull_request_target` 认证工作流。

## [1.3.0] - 2026-07-29

### 新增

- 增加可选本地 Obsidian 大脑协议，任一 22408 Skill 都会在配置有效时自动检索并结构化沉淀长期学习记忆。
- 增加本机配置与幂等初始化脚本，支持 `configure`、`check`、`enable`、`disable` 和 `--dry-run`。
- 增加考研专属学习档案、当前进度、错题队列、记忆索引和知识笔记目录。
- 为 12 个 Skill 各增加一个 Obsidian 行为场景，正式行为门禁扩展至 `36/36`。

### 调整

- 公共输出顺序增加透明的 Obsidian 连接、读取和写入状态；便携 Schema 1.1 JSON 仍保持为最后一个内容块。
- 内部 evidence schema 升级为 1.3；路由门禁继续保持 `60/60`。
- 隐私政策明确本地 Vault 由用户和宿主管理，发布者不接收配置、笔记或学习记录。

## [1.2.0] - 2026-07-26

### 新增

- 增加复合意图优先级与固定三行 Markdown 交接卡，确保官方阻塞事实先核验、当前主要产物唯一主责、下游任务最小交接。
- 将动态评测扩展为每个 Skill 恰好 5 个路由场景，共 `60/60`；多轮行为场景扩展为 `24/24`，签名清单覆盖 108 份结构化响应摘要。
- 增加中断恢复、错因假设证伪、延迟掌握、答案冲突、数学独立复核、英语证据链、政治理论/时政分离和真题小样本边界评测。

### 调整

- 12 个 Skills 统一采用“结论/立即行动 → 必要依据或推导 → 可选交接卡 → 必需记录 JSON”的自适应简洁输出。
- 规划、执行、诊断和错题闭环分别强化配额闭合、分钟闭合、观察/推断分离和延迟掌握判据。
- 408、数学二、英语二和政治采用更严格的学科推导、复核、证据与批改结构。
- 模考交卷后先回显最终作答映射，再依据冻结题面与公开 rubric 评分；交卷前继续保持零泄露。
- 真题与材料增加覆盖表、缺页/分块声明和趋势置信边界；官方核验增加来源冲突与文件有效性比较。
- 内部 evidence schema 升级为 1.2；便携学习记录继续使用 Schema 1.1，并保持 1.0 兼容迁移。
- Actions 升级为完整 SHA 固定的 Node 24 版本，历史 evidence 1.1 门禁继续受保护。

## [1.1.0] - 2026-07-18

### 新增

- 增加便携学习记录 Schema 1.1 与机器可验证 JSON Schema，并兼容读取 1.0 记录。
- 增加跨平台 Python 安装器、确定的退出码和 PowerShell 薄包装。
- 增加 12 个多轮行为场景，与 36 个路由场景共同构成发布前动态评测门禁。
- 增加可重复 Release 构建、SHA-256 文件与 Windows/Ubuntu 字节一致性检查。
- 增加离线 OpenSSH 分离签名、逐响应摘要清单和受保护基分支验证工作流，阻止候选分支用自带公钥为评测证据自证明。

### 调整

- `kaoyan-review-executor` 现在可在没有长期计划时，根据本次目标、科目与时长安排单次学习时段。
- 统一 12 个 Skills 的正向与邻近负向路由，材料按用户意图而不是文件类型分流。
- 修正模考流程：用户自行计时；题面、版本、分值和通用 rubric 在出卷时冻结；明确交卷前不泄露答案或得分线索。
- 统一用户材料授权边界、证据标签和官方一手来源范围。
- GitHub 仓库成为唯一 marketplace 分发源；Release ZIP 只作为可审计、可校验的插件包。
- 本地安装器现在要求同名 marketplace 的规范化根路径与当前仓库完全一致，拒绝路径后缀或重复来源混淆。
- 本地安装器在 GBK/ASCII 等控制台中安全转义不可编码的 validator 日志字符，避免校验通过后因输出编码崩溃。
- 密钥扫描补充加密私钥、PGP 私钥、npm Token 与 GitLab Personal Access Token 类别，并覆盖当前发布树、完整 Git 历史和 Semgrep。
- 普通 CI 中的仓库内前向证据检查明确降级为非权威一致性检查；Release 必须通过仓库外固定公钥的认证门禁。
- 行为评测输出 Schema 改用 Codex 结构化输出支持的关键字子集，最终 evidence schema 继续负责唯一性约束。
- 行为评测 judge 现在同时读取原始会话，并逐字复用 rubric 文本，避免因缺少用户输入上下文或自动编号造成误判。
- 模考交卷指令强制要求回报用时；私有内容拒绝必须明确覆盖搜索、重建、跨会话记忆和后台学习状态边界。
- 408 讲解必须拆分用户材料与模型扩展；进度诊断不得把“本周”等相对周期擅自推定为绝对日期。

### 移除

- 移除与 GitHub repo marketplace 分发无关的提交流程材料。

## [1.0.0] - 2026-07-15

- 首次公开发布 12 个中文 Skills。
- 采用纯 Skills 架构，无 App、MCP、后台服务、账号系统或数据持久化。
- 不内置第三方题库、讲义、真题合集或解析库。

[2.0.0]: https://github.com/yq6666-66/kaoyan-22408-codex-plugin/compare/v1.4.0...v2.0.0
[1.4.0]: https://github.com/yq6666-66/kaoyan-22408-codex-plugin/compare/v1.3.0...v1.4.0
[1.3.0]: https://github.com/yq6666-66/kaoyan-22408-codex-plugin/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/yq6666-66/kaoyan-22408-codex-plugin/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/yq6666-66/kaoyan-22408-codex-plugin/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/yq6666-66/kaoyan-22408-codex-plugin/releases/tag/v1.0.0
